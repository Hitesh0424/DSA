public class substringss {
    public static void main(String arg[]){
        // use .substric function to get a substring from any string 
        // substring (beginnning index, ending index)
        String name = "jai MAharashtra";
        String sub = name.substring(2, name.length());
        System.out.println(sub);

    }
}
